apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quant-ux-mongo-claim0
  labels:
    app: quant-ux-mongo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
